{
  "name": "jotsy",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Eric Morrison",
  "keywords": [
    "log",
    "logger",
    "logging",
    "formatting",
    "terminal",
    "console",
    "print"
  ],
  "description": "A cleaner console.log.",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx nodemon",
    "test": "echo 'Testing?'",
    "build": "npx rimraf ./build && npx tsc",
    "prepublish": "yarn test"
  },
  "dependencies": {
    "husky": "^4.2.3"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "@types/jest": "^25.1.4",
    "cz-conventional-changelog": "^3.1.0",
    "prettier": "^1.19.1",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
